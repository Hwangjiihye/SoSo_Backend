<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Login">
	
	<select id="toLogin" resultType="map">
    SELECT 
        u.user_seq,
        u.password,
        u.user_type,
        u.status,
        u.nickname,
        s.company_name  FROM users u
    LEFT JOIN stores s ON u.user_seq = s.user_seq  WHERE u.user_id = #{id} 
      AND u.user_type = #{user_type}
</select>
	
	 
</mapper>